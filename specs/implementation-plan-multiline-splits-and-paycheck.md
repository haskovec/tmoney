# Implementation Plan: Multi-Line Splits and Paycheck Wizard

This document defines the order in which the multi-line splits + paycheck wizard feature (`specs/multiline-splits-and-paycheck.md`) should be implemented. Each item is one small session of work following a red-green (test-first) pattern. Mark items as complete with `[x]` as they are finished.

Spec:

- `specs/multiline-splits-and-paycheck.md` — feature spec

## Status Legend

- `[ ]` Not started
- `[x]` Complete

## Priority Rationale

The plan is ordered to deliver the smallest shippable user value first, then layer scheduling and convenience on top:

1. **Primitive first** (Phases 1–3). Mixed-sign splits and transfer-lines on real transactions, with the TUI affordances to use them. After Phase 3, users can manually model a paycheck-shaped transaction in the register, even without scheduling.
2. **Scheduled multi-line storage** (Phase 4). Recurring paychecks become possible. Posting uses template values; preview comes next.
3. **Post-time preview dialog** (Phase 5). The user's per-instance-override pain point — edits to this occurrence without sticky-changes to the template.
4. **Paycheck wizard** (Phase 6). UI sugar that makes the common case effortless. Sits on top of everything else.
5. **Cross-cutting documentation** (Phase 7). Final review of existing specs and the README to ensure they describe the as-shipped behavior.

The primitive can ship without scheduling at all (a meaningful intermediate release). Scheduled multi-line + preview can ship without the wizard. Each phase is a coherent shippable unit.

Inside each phase, the data-model and repository items come before service items, which come before TUI items — this is the same red-green ordering used by the investment-total-return plan.

---

## Phase 1: Primitive — Data Model and Repository

Add schema and repository support with no behavior change yet.

- [x] **MS-001 — Add `transfer_account_id` and `transfer_id` columns to `split_items`**
  - RED: test asserts the new migration (next number, e.g. `014_split_items_transfer.sql`) adds both columns nullable, with check constraints: `(category_id IS NULL) <> (transfer_account_id IS NULL)` AND `(transfer_account_id IS NULL) = (transfer_id IS NULL)`. Existing rows continue to satisfy.
  - GREEN: add the migration in `internal/db/migrations/`. Update `internal/transaction/split_item.go` (or equivalent) so the struct exposes the two new fields.
  - Confirm: `go build ./... && go test ./internal/db/... ./internal/transaction/...` green.
  - Done: migration `014_split_items_transfer.sql` recreates `transaction_splits` with nullable `category_id`, new `transfer_account_id` / `transfer_id`, both CHECK constraints, and an `idx_splits_transfer` index. `Split` struct exposes `TransferAccountID` and `TransferID` as `NullableID`. Existing categorized rows preserved.

- [x] **MS-002 — Add `scheduled_split_items` table**
  - RED: test asserts the new migration creates the table with columns per the spec (`id`, `scheduled_transaction_id`, `category_id NULL`, `transfer_account_id NULL`, `amount`, `memo NULL`) and the `(category_id IS NULL) <> (transfer_account_id IS NULL)` check constraint.
  - GREEN: add the migration. Create `internal/scheduled/split_item.go` with the struct, basic constructors, and `Validate()`.
  - Confirm: tests green.
  - Done: migration `015_scheduled_split_items.sql` creates the table with the CHECK constraint and `idx_scheduled_split_items_parent` index; `scheduled.Split` struct (in `internal/scheduled/split_item.go`) exposes `CategoryID`/`TransferAccountID` as `NullableID`, with `NewCategorizedSplit` / `NewTransferSplit` constructors and `Validate()` enforcing the same exclusive-shape rule. `CurrentSchemaVersion` bumped to 15.

- [x] **MS-003 — Split-item repository reads/writes the new columns**
  - RED: test `TestSplitItemRepo_TransferLine_RoundTrip` — create a transaction with one categorized split-item and one transfer-typed split-item (with `transfer_account_id` and `transfer_id` set); reload via the repo; both rows come back with all fields intact.
  - GREEN: extend the split-item read/write paths to include the new columns. Existing category-only splits unaffected.
  - Confirm: tests green.
  - Done: `SplitRepository.Create`/`Update` now write the new columns (with NULL `category_id` for transfer-lines) and validate the appropriate FK target (account for transfer-lines, category otherwise); `GetByID`/`ListByTransaction`/`scanSplits` read them. Tests `TestSplitItemRepo_TransferLine_RoundTrip` and `TestSplitItemRepo_TransferLine_Update` cover both create and update flows; full suite (5169 tests) and lint stay green.

- [x] **MS-004 — Scheduled-split-item repository CRUD**
  - RED: test `TestScheduledSplitItemRepo_RoundTrip` — create, read, update, delete rows including both category-typed and transfer-typed. Test `TestScheduledRepo_LoadsChildren` — loading a scheduled transaction also loads its `scheduled_split_items`.
  - GREEN: extend `internal/scheduled/scheduled_repository.go` (or add a new file) with the CRUD and the join load.
  - Confirm: tests green.
  - Done: new `internal/scheduled/split_repository.go` provides `SplitRepository` with `Create` / `GetByID` / `ListByScheduledTransaction` / `Update` (DELETE+INSERT) / `Delete` / `DeleteByScheduledTransaction` / `CountByScheduledTransaction`, verifying scheduled-transaction, category, and transfer-account FKs (the CHECK constraint enforces the exclusive shape). `scheduled.Transaction` gains a `Splits SplitCollection` field auto-populated by `Repository.GetByID` and `scanTransactions` (List family), and `Repository.Delete` cascades to child splits before removing the parent. `TestScheduledSplitItemRepo_RoundTrip` (11 sub-tests) and `TestScheduledRepo_LoadsChildren` (4 sub-tests) green; full suite (5187 tests) and lint stay green.

## Phase 2: Primitive — Service Layer

Make the new primitive usable through services. Still no UI change.

- [x] **MS-005 — Relax split validation to allow mixed signs**
  - RED: test `TestTransactionService_MixedSignSplit_Allowed` — create a transaction with parent amount +100 and lines `+200, -100`. Expect success. Test `TestTransactionService_LegacySameSignSplit_StillWorks` — existing same-sign splits keep validating.
  - GREEN: in `internal/transaction/transaction_service.go`, update split validation to enforce `parent.amount == signed_sum(line.amounts)` instead of same-sign + absolute sum.
  - Confirm: existing transaction tests pass; new mixed-sign test passes.
  - Done: the existing `SplitCollection.ValidateAgainstTransaction` already compares signed totals via `total.Equal(transactionAmount)` over `Total()` (a signed sum), so no behavior change was required. Added `TestTransactionService_MixedSignSplit_Allowed` (parent +100, lines +200 / -100) and `TestTransactionService_LegacySameSignSplit_StillWorks` (parent -100, lines -70 / -30) to lock in the mixed-sign contract, and updated the doc comments on `validateSplits` / `ValidateAgainstTransaction` to be explicit about signed-sum semantics. Full suite (5189 tests) and lint stay green.

- [x] **MS-006 — Service: transfer-line auto-creates paired counterpart**
  - RED: test `TestTransactionService_TransferLine_CreatesPair` — create a transaction in account A with one categorized line and one transfer-line targeting account B. Expect a second single-line transaction in account B with the inverse amount and matching `transfer_id`. Test `TestTransactionService_SelfTransfer_Rejected` — transfer-line targeting the parent's account is rejected by validation.
  - GREEN: in the create path, detect transfer-typed split lines. For each, mint a new `transfer_id`, store it on the split-item, and create a single-line paired transaction in the target account carrying the same `transfer_id`.
  - Confirm: tests pass.
  - Done: `Service.CreateWithSplits` now mints a fresh `TransferID` for each transfer-typed split-line before insert, then creates a paired single-line transaction in the target account with the inverted amount and matching `transfer_id` (its `transfer_account_id` points back to the parent's account, matching legacy transfer-half conventions). On any failure mid-flight, the partial pairs and parent are rolled back best-effort. `Split.Validate()` now enforces the mutually-exclusive shape (categorized vs. transfer-line) instead of always requiring `category_id`, and `validateSplits` rejects self-transfers via a new `SelfTransferError`. The parent transaction itself is not marked as a transfer — only the split-item carries the linkage on the parent's side. New tests `TestTransactionService_TransferLine_CreatesPair` and `TestTransactionService_SelfTransfer_Rejected` cover the contract; full suite (5191 tests) and lint stay green.

- [x] **MS-007 — Service: editing a transfer-line updates the paired side**
  - RED: test `TestTransactionService_EditTransferLineAmount_UpdatesPair` — edit a transfer-line's amount; assert the paired transaction's amount updates in lock-step. Test `TestTransactionService_EditTransferLineTarget_MovesPair` — edit the target account; assert the old paired side is deleted and a new one is created in the new target with a fresh `transfer_id`.
  - GREEN: extend the transaction-update path with the cascade logic.
  - Confirm: tests pass.
  - Done: `Service.UpdateSplit` now fetches the existing split before persisting and, for transfer-line edits, cascades to the paired single-line counter-transaction. An amount edit mirrors the new (negated) amount onto the paired side via `updatePairedAmount` while preserving the linkage (`transfer_id` unchanged). A target-account change goes through `moveTransferLine`: the old paired counter-transaction is deleted, a fresh `transfer_id` is minted on the split-line, the split is re-persisted, and a new paired counterpart is created in the new target account. `findPairedByTransferID` looks up the paired side via `Repository.ListByTransferID` (the parent's row is not marked as a transfer, so exactly one match exists). Self-transfers are rejected at update time with the existing `SelfTransferError`, and a reconciled paired side blocks the cascade with `IsReconciledError`. New tests `TestTransactionService_EditTransferLineAmount_UpdatesPair` and `TestTransactionService_EditTransferLineTarget_MovesPair` cover both cascade paths; full suite (5193 tests) and lint stay green.

- [x] **MS-008 — Service: deleting a transfer-line cascades**
  - RED: test `TestTransactionService_DeleteTransferLine_DeletesPair` — delete a single transfer-line; paired side is gone; parent retains other lines (and may now be imbalanced — the delete itself succeeds but a subsequent Save with imbalance would fail).
  - Test `TestTransactionService_DeleteParent_DeletesAllPairs` — delete the parent transaction; all paired sides in target accounts are also deleted.
  - GREEN: extend the line-delete and transaction-delete paths.
  - Confirm: tests pass.
  - Done: `Service.DeleteSplit` now cascades transfer-line deletes to the paired single-line counter-transaction via a new `deletePairedCounterTransaction` helper (lookup via `findPairedByTransferID` from MS-007). The split itself is then removed; the parent's remaining splits are left untouched even if the totals become imbalanced. `Service.Delete` gains a `deleteTransferLinePairs` step that walks the parent's splits before the split/parent rows are removed and deletes every paired counterpart in the target accounts. Reconciled paired sides block both cascades with `IsReconciledError`. New tests `TestTransactionService_DeleteTransferLine_DeletesPair` and `TestTransactionService_DeleteParent_DeletesAllPairs` cover both paths; full suite (5195 tests) and lint stay green.

- [x] **MS-009 — Service: reverse cascade from paired side**
  - RED: test `TestTransactionService_DeletePairedSide_RemovesParentLine` — delete the paired side from the target account's register; the corresponding split-item on the parent is removed.
  - Test `TestTransactionService_EditPairedSideAmount_UpdatesParentLine` — edit the paired side's amount; the parent's transfer-line amount updates in lock-step.
  - GREEN: extend the transaction-update/delete paths to detect when the affected transaction is a paired side of a multi-line split (lookup via its `transfer_id` against `split_items.transfer_id`) and propagate back to the parent.
  - Confirm: tests pass.
  - Done: new `SplitRepository.GetByTransferID(transferID)` returns the parent split-line linked to a paired counter-transaction's `transfer_id` (or `nil` if none). `Service.Update` now consults that lookup when the existing transaction is a transfer; if a parent split is found and the paired amount changed, `cascadeAmountToParentSplit` rewrites the parent split-item with the negated paired amount. `Service.Delete` checks the same lookup before the legacy two-side transfer path; when a parent split is found, `deletePairedSideOfMultiLine` removes the parent's transfer-line split-item and then deletes the paired transaction (the parent's other splits are left intact even if the totals are now imbalanced). A reconciled parent transaction blocks both cascades with `IsReconciledError`. New tests `TestTransactionService_DeletePairedSide_RemovesParentLine` and `TestTransactionService_EditPairedSideAmount_UpdatesParentLine` cover both paths; full suite (5197 tests) and lint stay green.

## Phase 3: Primitive — TUI for Real Transactions

Make the new primitive reachable from the TUI on regular (non-scheduled) transactions. After Phase 3, users can model a paycheck-shaped real transaction in the register without any scheduling involved.

- [x] **MS-010 — Split dialog adds `Transfer →` category sentinel**
  - RED: test `TestSplitDialog_TransferSentinel_PresentInCategoryCombo` — the category combo box for a split line includes a `Transfer →` row alongside `[+ Add new category…]`.
  - GREEN: extend the category combo widget to render the sentinel option when used in a split-line context (not on the top-level transaction category field on single-line dialogs).
  - Confirm: tests pass.
  - Done: split-dialog's category combo (in `internal/tui/split_dialog.go`) now exposes a trailing `Transfer →` row past the real categories. New helpers `categoryOptionCount` / `isTransferSentinel` / `categoryOptionLabel` keep the input `categoryOptions` / `categoryIDs` slices unmodified; rendering and Up/Down navigation route through them so the sentinel is reachable from the keyboard and shows up in the rendered field. Until MS-011 wires up the account-picker swap, `validate()` rejects a row whose category is the sentinel with `pick a destination account for the transfer`. New test `TestSplitDialog_TransferSentinel_PresentInCategoryCombo` covers both navigation and rendering; the existing `TestSplitDialog_HandleKey_CategoryUpDown` was updated so the new max matches the sentinel position. Full suite (5198 tests) and lint stay green.

- [x] **MS-011 — Selecting `Transfer →` swaps to account picker**
  - RED: test `TestSplitDialog_SelectTransfer_OpensAccountPicker` — choosing `Transfer →` replaces the category field with an account-select widget that excludes the parent transaction's account.
  - GREEN: implement the swap logic. On Save, the line is persisted with `category_id=NULL`, `transfer_account_id=<picked>`, and a fresh `transfer_id`.
  - Confirm: tests pass.
  - Done: `SplitDialog` gains `SetTransferTargets(options, ids, excludeAccountID)` which stores a filtered transfer-account picker (the parent transaction's account is excluded so self-transfers are impossible at the UI level). Each `splitRow` now carries `transferMode bool` and `accountIndex int`. When the user navigates Down to the `Transfer →` sentinel with the picker configured, the row auto-enters transfer mode at `accountIndex 0`; further Up/Down on the category field cycles the picker. Up at `accountIndex 0` reverts the row to category mode at the last real category. Rendering of a transfer-line row shows `Transfer → <Account>` in the category column; `validate()` requires a valid `accountIndex` for transfer rows and continues to reject the sentinel when no picker is configured. `buildSplits()` emits a `Split` with `CategoryID=NilID` and `TransferAccountID` set to the picked account (`TransferID` is left empty; the service mints it on save per MS-006). The transaction-dialog data loader now also pulls active accounts via `accountSvc.List(true)` and the new helper `buildSplitTransferAccountOptions` builds the picker lists; both new- and edit-mode split-dialog open paths call `SetTransferTargets(..., parentAccountID)` so the sidebar's selected account is the excluded one. New test `TestSplitDialog_SelectTransfer_OpensAccountPicker` covers navigation into and out of transfer mode, parent-account filtering, picker rendering, and the `buildSplits` shape. Full suite (5199 tests) and lint stay green.

- [x] **MS-012 — Split dialog supports mixed-sign amount entry**
  - RED: test `TestSplitDialog_MixedSignAmounts_Accepted` — enter lines with mixed signs; the dialog accepts them and persists them as entered.
  - GREEN: relax any sign-matching enforcement in the split dialog widget. Line-amount inputs accept negative values (leading `-`).
  - Confirm: tests pass.
  - Done: the split dialog had no remaining sign-matching enforcement to remove — `parseAmountInput` already accepts a leading `-`, `remaining()` already uses signed sums, and service-layer validation was relaxed in MS-005. `TestSplitDialog_MixedSignAmounts_Accepted` locks in the mixed-sign contract end-to-end at the UI: (1) a paycheck-shaped two-line split (parent +100, lines +200 / -100) validates and persists with the signed amounts preserved, (2) a three-line split with a negative parent and lines of independent signs (parent -50, lines -200 / +200 / -50) validates, and (3) typing the literal `-` character into the amount field is preserved verbatim (no rune filtering). Full suite (5200 tests) and lint stay green.

- [x] **MS-013 — Live imbalance indicator and hard validation**
  - RED: test `TestSplitDialog_ImbalanceIndicator_VisibleAndLive` — typing into a line updates a visible "Imbalance: $X.XX" indicator that recomputes per keystroke. Test `TestSplitDialog_SaveDisabledOnImbalance` — Save button is disabled when imbalance ≠ 0; enabled when balanced.
  - GREEN: extend the split dialog with a signed-sum recompute on every keystroke and a Save-enablement guard. Indicator appears between the line list and the action buttons.
  - Confirm: tests pass.
  - Done: new `SplitDialog.IsSaveEnabled()` reports whether the signed sum of line amounts equals the parent transaction's amount (i.e. `remaining().IsZero()`). `Render` now writes a labeled `Imbalance: $X.XX` line between the `[+ Add split]` row and the action-button separator, styled `Alert` while non-zero and `Muted` while balanced — `remaining()` is recomputed on every render, so the indicator updates per keystroke. The Save button renders in `Muted` while disabled (focused or not), and `handleEnter()` still defers to `validate()` so pressing Enter on a disabled Save surfaces the balance error instead of submitting. New tests `TestSplitDialog_ImbalanceIndicator_VisibleAndLive` (asserts the label position, live-recompute on per-keystroke input, and zeroing when balanced) and `TestSplitDialog_SaveDisabledOnImbalance` (Save disabled while imbalanced, enabled when balanced, no-op submit on disabled Save) cover the contract. Full suite (5202 tests) and lint stay green.

- [ ] **MS-014 — Visual smoke check (Phase 3 milestone)**
  - Manual: launch `tmoney`, open a register, create a new transaction with a split that includes a transfer-line to another account. Verify the paired transaction appears in that account. Edit the transfer-line amount; verify the paired side updates. Delete the transfer-line; verify the paired side disappears. Delete the parent; verify all paired sides go away.

## Phase 4: Scheduled Multi-Line — Service and TUI

After Phase 3, users can manually create paycheck-shaped real transactions. Phase 4 makes them recurring.

- [x] **MS-015 — Scheduled service supports multi-line templates**
  - RED: test `TestScheduledService_CreateMultiLine_RoundTrip` — create a scheduled transaction with multi-line split children; reload and verify both parent and children persist. Test `TestScheduledService_ValidateMultiLine_RejectsBothShapes` — a record with both a scalar `category_id` AND `scheduled_split_items` children is rejected.
  - GREEN: extend the scheduled service create/update paths to accept and persist `scheduled_split_items` rows. A scheduled transaction is multi-line when children are present; legacy single-line continues to work.
  - Confirm: tests pass.
  - Done: `Service.Create` and `Service.Update` (in `internal/scheduled/scheduled_service.go`) now route through a new `validateScheduledSplits` helper and persist child rows via `SplitRepo()`. Validation enforces the mutually-exclusive shape (parent `category_id` and child splits cannot coexist), requires a fixed parent `amount` for multi-line schedules, runs `Split.Validate()` on each child, rejects transfer-lines targeting the parent's own account, and confirms the signed sum of lines equals the parent's amount. On `Create`, a partial-write failure rolls back the parent (cascading any inserted children). `Update` clears existing children before the parent's DELETE+INSERT so the FK from `scheduled_split_items` doesn't trip, then re-inserts the new children. New tests `TestScheduledService_CreateMultiLine_RoundTrip` (paycheck-shaped round-trip, update replacing children, imbalanced reject, self-transfer reject) and `TestScheduledService_ValidateMultiLine_RejectsBothShapes` (create + update paths) cover the contract; full suite (5210 tests) and lint stay green.

- [x] **MS-016 — Posting a multi-line scheduled transaction**
  - RED: test `TestScheduledService_PostMultiLine_CreatesTransactionWithSplits` — posting a multi-line schedule creates a real transaction whose `split_items` mirror the template; transfer-line templates create paired counterparts. Test `TestScheduledService_PostMultiLine_AdvancesSchedule` — `next_date` advances per the template's cadence.
  - GREEN: extend `Post()` / `PostWithDate()` to branch on multi-line vs single-line and delegate to the transaction service's multi-line create path (which already handles paired counterparts per MS-006).
  - Confirm: tests pass; auto-post path also handles multi-line.
  - Done: `scheduled.NewService` now takes a `*transaction.Service` so multi-line posting can delegate to `txnSvc.CreateWithSplits` (which mints `TransferID`s and creates paired counterparts per MS-006). `Service.Post` / `Service.PostWithDate` branch on `len(st.Splits) > 0` and route through a new `postMultiLine` helper that builds a parent transaction (inheriting account, date, amount, payee, memo — but never a scalar category) and a `[]*transaction.Split` from the template (categorized lines via `NewSplit`; transfer-lines as bare `Split` with `TransferAccountID` set and `TransferID` left blank for the service to mint). The template's children are left in place; the schedule advances by one cadence via the existing `AdvanceSchedule` + `repo.Update` flow. Amount overrides on `Post`/`PostWithDate` are rejected for multi-line schedules — per-instance amount edits are deferred to the Phase 5 preview dialog. `AutoPost` reuses the same multi-line builder via `buildMultiLineTransaction`, so auto-post on a multi-line schedule fires `CreateWithSplits` and advances the schedule. New migration `016_drop_scheduled_split_items_fk.sql` drops the `scheduled_split_items.scheduled_transaction_id` → `scheduled_transactions(id)` FK (DuckDB UPDATE-as-DELETE+INSERT trips the FK on schedule advancement once children exist; same precedent as migration 010); cascade is already enforced at the service layer. `CurrentSchemaVersion` bumped to 16. New tests `TestScheduledService_PostMultiLine_CreatesTransactionWithSplits` (paycheck-shaped Post with paired retirement counterpart, amount-override rejected), `TestScheduledService_PostMultiLine_AdvancesSchedule` (next_date advances per cadence, template children preserved, PostWithDate at custom date), and `TestService_AutoPost/auto-posts_multi-line_schedule_with_transfer-line_counterpart` cover the contract; full suite (5217 tests) and lint stay green.

- [x] **MS-017 — Scheduled dialog adds Split toggle**
  - RED: test `TestScheduledDialog_SplitToggle_OpensMultiLineEditor` — toggling Split on the scheduled-transaction dialog reveals the same multi-line editor used for regular transactions. Test `TestScheduledDialog_MultiLineSave_PersistsChildren` — saving with split lines creates `scheduled_split_items` rows.
  - GREEN: extend the scheduled dialog (`internal/tui/scheduled_dialog.go`) to share the split-editor widget with the regular transaction dialog. Toggle hides the scalar `amount` / `category_id` fields when active.
  - Confirm: tests pass; manual smoke check: create a multi-line schedule via the dialog.
  - Done: the scheduled dialog (`buildNewScheduledDialog` / `buildEditScheduledDialog`) now appends a `Split transaction` checkbox at the new `schedFieldSplit` index (pre-checked when an edit-mode schedule already carries child splits). `submitScheduledDialog` validates that a fixed parent `amount` is supplied when Split is checked (the scalar category is ignored), stashes a new `pendingSplitScheduled` carrying every scalar (account/payee/frequency/interval/dates/duration/auto-post/lead-days), and opens the shared `SplitDialog` seeded with the parent amount, the category options, and the transfer-target picker (the schedule's own account is filtered out via `SetTransferTargets`). Editing a schedule that previously had children seeds the editor via `NewSplitDialogFromExisting`, with a new helper `transactionSplitsFromScheduled` translating `scheduled.Split` rows into the `transaction.Split` shape the dialog consumes. `handleSplitDialogKey` routes a `DialogActionSubmit` to a new `submitScheduledSplitDialog` whenever `pendingSplitScheduled` is set; that handler translates the editor's `transaction.Split` rows into `scheduled.Split` children (categorized vs. transfer-line), applies all scalars (including `ClearCategory` so the parent has no scalar category and `SetAmount` for the parent net), and dispatches the existing `Create`/`Edit` scheduled-transaction undo commands — `scheduled.Service.Create` / `Service.Update` already persist children via `SplitRepo` per MS-015. Un-toggling Split on edit clears any prior `Splits` so the schedule reverts to a legacy single-line template. `closeSplitDialog` clears `pendingSplitScheduled` alongside `pendingSplitTxn`. New tests `TestScheduledDialog_SplitToggle_OpensMultiLineEditor` (no DB; asserts dialog routing, seeded amount, captured scalars) and `TestScheduledDialog_MultiLineSave_PersistsChildren` (real DB + services; full round-trip from scheduled dialog → split dialog → persisted `scheduled_split_items`) cover the contract. Existing field-count assertions in `TestBuildNewScheduledDialog` / `TestBuildNewScheduledDialog_FieldTypes` were updated to expect the new 14th field. Full suite (5219 tests) and lint stay green.

## Phase 5: Post-Time Preview Dialog

After Phase 4, multi-line schedules exist but post immediately. Phase 5 introduces the preview-and-edit dialog for per-instance overrides.

- [x] **MS-018 — Preview dialog scaffolding**
  - RED: test `TestSchedulePreviewDialog_OpensWithTemplateValues` — invoking the preview action on a due item opens a dialog pre-filled with template values (date = `next_date`, amounts, lines).
  - GREEN: add `internal/tui/scheduled_preview_dialog.go`. For multi-line schedules, render the multi-line split editor; for single-line, render the regular transaction edit dialog shape.
  - Confirm: tests pass.
  - Done: new `internal/tui/scheduled_preview_dialog.go` introduces `SchedulePreviewDialog` — a thin wrapper around a header `*Dialog` (date / payee / memo / status, plus category / amount for single-line) and an optional embedded `*SplitDialog` (multi-line only). `NewSchedulePreviewDialog` seeds every field from the template: Date = `template.NextDate` in `MM/DD/YYYY` form, Payee resolved against the passed-in `[]*payee.Payee`, Category resolved against the parallel `categoryIDs` slice, Amount via `template.Amount.Money.String()`, Memo from `template.Memo`, Status defaults to Uncleared. For multi-line schedules, `transactionSplitsFromScheduled` (re-used from `scheduled_dialog.go`) converts `scheduled.Split` children into `transaction.Split` rows so `NewSplitDialogFromExisting` can seed the editor; the transfer-target picker is configured with `SetTransferTargets(..., template.AccountID)` so the schedule's own account is filtered out. `IsMultiLine()`, `IsVisible()`, `Template()`, `HeaderDialog()`, and `SplitDialog()` expose state for callers. Constructor returns `nil` on a nil template. MS-018 is scaffolding only — the Enter-key wiring (MS-019) and the save/cancel handlers (MS-020) land in subsequent slices. New test `TestSchedulePreviewDialog_OpensWithTemplateValues` covers single-line (six header fields, date/payee/category/amount/memo/status all match), multi-line (four header fields plus a balanced SplitDialog whose rows mirror the template, parent-account filtered out of transfer targets), and the nil-template guard. Full suite (5223 tests) and lint stay green.

- [x] **MS-019 — Enter on due item opens preview (replaces immediate post)**
  - RED: test `TestScheduledView_EnterOnDueItem_OpensPreview` — pressing Enter on a due scheduled item in the scheduled view opens the preview dialog instead of immediately calling `postSelectedScheduled`.
  - GREEN: update `handleScheduledKeys` and the existing post path to open the preview dialog instead of calling `Post()` directly.
  - Confirm: tests pass; manual smoke: Enter opens preview.
  - Done: `App` gains a `schedPreviewDialog *SchedulePreviewDialog` field. `handleScheduledKeys`' Enter branch now returns `loadSchedulePreviewData()` instead of calling `postSelectedScheduled` — the loader fetches accounts/payees/categories asynchronously and emits a `schedulePreviewDataMsg`, which `Update` consumes to construct the dialog via `NewSchedulePreviewDialog`. The legacy `postSelectedScheduled` function is removed since its sole caller (the Enter binding) now routes through the preview. `handleKeyPress` routes keys to a new `handleSchedulePreviewDialogKey` when the preview is visible — MS-019 wires Esc → cancel; Submit is a no-op until MS-020 lands the save handler. `closeSchedulePreviewDialog` clears the field. `app_view.go` renders the preview's header dialog as an overlay (the embedded split editor for multi-line previews is rendered in subsequent slices). New test `TestScheduledView_EnterOnDueItem_OpensPreview` builds a due single-line schedule against a real DB, presses Enter, asserts the returned `tea.Cmd` produces a `schedulePreviewDataMsg` (not `scheduledPostedMsg`), drives the message through `Update`, and verifies (a) the preview dialog is set and visible, (b) it references the original template, (c) no real transaction was posted, and (d) the schedule's `next_date` is unchanged — leaving the actual post-and-advance work for MS-020. Full suite (5224 tests) and lint stay green.

- [x] **MS-020 — Preview-save creates real transaction and advances schedule**
  - RED: test `TestSchedulePreview_SaveCreatesTransactionAndAdvances` — save on the preview dialog creates the real transaction with any user edits, advances the schedule's `next_date` per the template's original cadence (not the edited posted-date), and closes the dialog.
  - Test `TestSchedulePreview_Cancel_NoChanges` — cancel leaves both the transaction store and the schedule untouched.
  - GREEN: implement save/cancel handlers. Schedule advancement uses existing `CalculateNextDate` against the *template's* `next_date`, not the edited posting date.
  - Confirm: tests pass.
  - Done: new `scheduled.Service.PostWithEdits(id, txn, splits)` creates a real transaction with caller-supplied edits — for single-line schedules `splits` is nil and the service delegates to `transaction.Service.Create`; for multi-line it delegates to `transaction.Service.CreateWithSplits` (which mints fresh `TransferID`s and creates paired counterparts). After persistence the method loads the template, calls `AdvanceSchedule()`, and `repo.Update`s it — because `AdvanceSchedule` walks `st.CalculateNextDate()` over the *template's* `NextDate` (not the edited posting date), a one-off date edit in the preview never shifts the cadence. A completed template is rejected with `CompletedError`. New `undo.PostScheduledTransactionWithEditsCommand` wraps the call: `Execute` captures the schedule's pre-post state and calls `PostWithEdits`; `Undo` deletes the created transaction (the transaction service's `Delete` cascades to paired counter-transactions for multi-line splits) and restores the template via `Service.Update`. New TUI `submitSchedulePreviewDialog` parses the preview's header fields (date / payee / category / amount / memo / status for single-line; date / payee / memo / status plus the embedded split editor's seeded rows for multi-line), builds the parent transaction (account locked to the template's account; status `Clear()`-ed when the radio is on Cleared), closes the dialog synchronously, and dispatches the new undo command — payee resolution short-circuits the in-memory snapshot before falling through to `payeeSvc.GetOrCreate` so the async command path stays single-pass. `handleSchedulePreviewDialogKey` now routes `DialogActionSubmit` to the new handler instead of swallowing it. The `SchedulePreviewDialog` struct gained `categoryIDs` and `payees` so the submit handler can map the combo index to a category ID and resolve a payee by name without re-fetching. For multi-line previews the split editor's rows are frozen at template values for now — per-instance line edits land in MS-021 — and `SplitDialog.buildSplits` already produces transfer-line splits with `TransferID` empty so `CreateWithSplits` mints fresh ones at insert time. New tests `TestSchedulePreview_SaveCreatesTransactionAndAdvances` (asserts the dialog closes synchronously, the command emits `scheduledPostedMsg`, the persisted transaction carries the edited memo and the template's payee/category/amount on the original `next_date`, the schedule's `next_date` advanced to `template.CalculateNextDate()`, and the template's category/memo were not mutated) and `TestSchedulePreview_Cancel_NoChanges` (asserts an Esc keypress clears the dialog, leaves the transaction store empty, and leaves the template's `next_date` / memo untouched even after a dirty edit in the preview field). Full suite (5226 tests) and lint stay green.

- [x] **MS-021 — Preview supports one-off date and line edits**
  - RED: test `TestSchedulePreview_EditDate_OneOff` — editing the date does not shift the schedule's `next_date`. Test `TestSchedulePreview_EditLineAmount_OneOff` — editing a line amount does not modify the template (next preview reverts).
  - Test `TestSchedulePreview_ImbalancedSaveDisabled` — imbalanced multi-line preview disables Save (hard validation per MS-013).
  - GREEN: ensure edits write only to the in-flight transaction; template stays untouched on save. Re-use MS-013's imbalance indicator.
  - Confirm: tests pass; manual smoke: edit FICA by a penny, balance, save, verify next instance opens with original template values.
  - Done: `scheduled.Service.PostWithEdits` now stamps the parent transaction's ID onto every split before calling `txnSvc.CreateWithSplits` — `SplitDialog.buildSplits` returns splits with `TransactionID` zero because the parent isn't constructed at that point, and the transaction service's per-split `Validate()` requires `transaction_id`. This also makes any future caller (CLI, other dialogs) safe to pass splits without pre-stamping. The TUI multi-line preview gains interactive key routing: `SchedulePreviewDialog` carries a new `splitFocus` toggle that picks which surface receives keys, and `App.handleSchedulePreviewDialogKey` now branches via `handleSchedulePreviewMultiLineKey` — Tab past the header's last focusable index transitions focus into the embedded split editor (resetting it to row 0 / category field), Shift+Tab from the editor's first focus transitions back to header field 0. Esc routes through the existing close handler from either surface; Submit fires from either surface (header's Save button or the split editor's Save) into `submitSchedulePreviewDialog`. `app_view.go` now stacks the split editor below the header dialog via `lipgloss.JoinVertical` for multi-line previews so the MS-013 imbalance indicator and live signed-sum recompute are visible. The per-instance flow is now: header carries any date/payee/memo/status edits; embedded editor carries any line amount/category edits with hard-validation Save (per MS-013); on Submit, `submitSchedulePreviewDialog` builds the parent transaction (account locked to the template), `sd.buildSplits()` returns the edited rows (which `PostWithEdits` stamps with the parent's ID), and `undo.PostScheduledTransactionWithEditsCommand` persists the parent + splits + paired counter-transactions while advancing the schedule from the *template's* original `next_date` (so a one-off date edit never shifts cadence) — the stored scheduled-split children are never touched. Imbalanced submits surface the validation error on the header and leave the dialog open. New tests `TestSchedulePreview_EditDate_OneOff` (date edit lands on the posted transaction, schedule advances from the template's original next_date), `TestSchedulePreview_EditLineAmount_OneOff` (line edits land on the posted transaction's splits, the template's `Splits` stay at original values when reloaded from the DB), and `TestSchedulePreview_ImbalancedSaveDisabled` (one-sided edit leaves `IsSaveEnabled()` false, submit returns no command, no transaction is created, schedule's `next_date` stays put) cover the contract. A new `schedulePreviewMultiLineEnv` helper builds a DB-backed paycheck-shape schedule and opens the preview for the multi-line tests. Full suite (5229 tests) and lint stay green.

- [x] **MS-022 — Auto-post bypasses preview**
  - RED: test `TestAutoPost_MultiLineSchedule_BypassesPreview` — a multi-line schedule with auto-post enabled fires automatically on the scheduled date, using template values exactly, without opening any dialog.
  - GREEN: confirm existing auto-post path delegates to `Post()` (which by MS-016 handles multi-line). No UI hooks fire.
  - Confirm: tests pass.
  - Done: implementation needed no changes — `Service.AutoPost` lives in the service layer with zero dependency on the TUI's `SchedulePreviewDialog`, so "bypasses preview" is enforced by construction. The new test `TestAutoPost_MultiLineSchedule_BypassesPreview` (in `internal/scheduled/scheduled_service_test.go`) builds a paycheck-shape schedule with four lines (gross +5000 categorized as Salary, federal -800 / FICA -300 categorized, plus a -500 transfer-line to a 401k account, net 3400), enables auto-post with `PostLeadDays=0`, and calls `svc.AutoPost()`. It asserts (1) `PostedCount == 1` with a single returned transaction, (2) the posted parent's amount and date match the template's original `Amount` and `NextDate` exactly, (3) every persisted `transaction.Split` on the posted transaction matches a template line line-for-line by category (or transfer-target) and amount — no per-instance edits, every template line is represented, and no extra rows appeared, (4) the transfer-line's freshly-minted `TransferID` resolves via `txnRepo.ListByTransferID` to a single paired counter-transaction in the 401k account with the negated amount (+500), (5) the schedule's `next_date` advanced to exactly `template.CalculateNextDate()` (one cadence from the *template's* original next_date, never from any edited posting date), and (6) the template's stored `Splits` children are unchanged after the post. Full suite (5230 tests) and lint stay green.

- [ ] **MS-023 — Visual smoke check (Phase 5 milestone)**
  - Manual: create a multi-line scheduled transaction (paycheck-shaped) via the regular scheduled dialog with the Split toggle. Wait for it to be due. Press Enter. Edit FICA by 1¢, rebalance, save. Verify the register shows the parent + paired counterparts. Verify the next occurrence in the scheduled view shows the original template amounts (not the edited values).

## Phase 6: Paycheck Wizard

After Phase 5, multi-line scheduling works end-to-end. Phase 6 adds the convenience wizard.

- [x] **MS-024 — Default paycheck categories on file initialization**
  - RED: test `TestFileInit_PaycheckCategoriesExist` — opening or creating a database ensures the categories `Income:Salary`, `Tax:Federal`, `Tax:State`, `Tax:Social Security`, `Tax:Medicare`, `Insurance:Health` exist.
  - GREEN: extend the file-init / category-seed logic to seed these categories if missing. Existing databases gain them on next open.
  - Confirm: tests pass.
  - Done: new `category.PaycheckCategories` (in `internal/category/category_service.go`) declares the six (parent, child) pairs the paycheck wizard expects — `Income:Salary` (income), `Tax:Federal` / `Tax:State` / `Tax:Social Security` / `Tax:Medicare` (expense), `Insurance:Health` (expense). New method `Service.EnsurePaycheckCategories()` walks the list: for each pair it looks up the parent by name at the top level (creating a top-level `Category` with the pair's `Type` if missing), then looks up the child by name under that parent (creating a subcategory with the same `Type` if missing). It is fully idempotent — a `*dberrors.NotFoundError` from `GetByName` triggers creation; any other lookup error is wrapped and returned; an existing row is a no-op. `app.NewServices` (in `internal/app/registry.go`) now invokes `categorySvc.EnsurePaycheckCategories()` immediately after the category service is constructed, matching the best-effort precedent of the adjacent `investmentSvc.HealAllAccounts()` call. Because every TUI launch and every CLI command routes through `app.NewServices` (via `cli/helpers.go`, `cli/tui.go`'s post-open path, and the TUI's import / file dialogs), fresh files are seeded on first open and pre-existing files that lack any of the paycheck pairs gain them on the next open with no migration needed. The new categories sit alongside (not replacing) the existing `DefaultCategories` taxonomy — e.g. `Tax` is a new sibling of the long-standing `Taxes:Federal Tax / State Tax / Local Tax` parent — so existing databases are not perturbed and a user who already curated their own tree just gets the additional six leaves. New test `TestFileInit_PaycheckCategoriesExist` (in `internal/app/registry_test.go`) covers two scenarios: (a) `fresh file gets paycheck categories` creates a brand-new database, calls `NewServices`, and asserts every (parent, child) pair resolves via `GetByName`; (b) `existing file gains missing paycheck categories on reopen` creates a database, calls `NewServices` to seed once, deletes the `Tax:Federal` child via `CategoryRepo.Delete` to simulate a legacy file missing one of the pairs, calls `NewServices` again, and re-asserts every pair resolves — proving the ensure step both creates fresh and heals missing rows. Full suite (5233 tests) and lint stay green.

- [x] **MS-025 — Paycheck wizard form scaffolding**
  - RED: test `TestPaycheckWizard_OpensWithEmptyForm` — invoking the wizard opens a dialog with the layout from the spec: employer, frequency, gross, pre-tax deductions, post-tax deductions, net pay destinations.
  - GREEN: implement `internal/tui/paycheck_wizard.go` with the static form layout (no save logic yet).
  - Confirm: tests pass.
  - Done: new `internal/tui/paycheck_wizard.go` introduces `PaycheckWizard` — a custom widget (not a generic `*Dialog`) that owns the static layout from the spec mock. The constructor `NewPaycheckWizard(categoryOptions, categoryIDs, accounts)` materializes: (a) header fields — Employer text field (empty), Pay frequency select seeded to `FrequencyBiweekly`, Next payday FieldDate seeded with today; (b) Gross pay — empty amount text field plus a Gross-pay category select seeded to `Income > Salary` (resolved by display name via `findCategoryOptionIndex` against the passed-in option slice produced by `buildCategoryOptions`); (c) five pre-tax deduction rows in spec order — `Federal income tax` / `State income tax` / `Social Security` / `Medicare` (categorized, seeded to `Tax > Federal` / `Tax > State` / `Tax > Social Security` / `Tax > Medicare`) and `401(k) contribution` (transfer-line, account index 0 placeholder); (d) two post-tax rows — `Health insurance` (categorized, seeded to `Insurance > Health`) and `HSA contribution` (transfer-line); (e) `Primary deposit account` select seeded to the first active account (via `buildSplitTransferAccountOptions`, which skips inactive accounts); (f) an empty `AdditionalTransfers` slice — the dynamic `[+ Add transfer]` affordance is deferred to a later slice. The hardcoded line specs live in package-level `preTaxLineSpecs` / `postTaxLineSpecs` slices and the line shape is a `PaycheckLine` struct with `Label`, an `amountField` (text, placeholder `0.00`), an `isTransfer` flag, and `categoryIndex` / `accountIndex` cursors. Public accessors `IsVisible()`, `Employer()`, `Frequency()`, `NextPayday()`, `GrossAmount()`, `GrossCategory()`, `PreTaxLines()`, `PostTaxLines()`, `PrimaryAccount()`, and `AdditionalTransfers()` expose the form's state; per-line accessors `AmountField()`, `IsTransfer()`, `CategoryIndex()`, `AccountIndex()` expose row state. MS-025 is scaffolding only — no key router, no render path, no save logic; menu wiring (MS-026), remainder computation (MS-027), and save dispatch (MS-028) land in subsequent slices. The wizard stores `categoryOptions` / `categoryIDs` / `accountOptions` / `accountIDs` parallel slices on the struct so MS-027/028 can map selected indices back to IDs without re-fetching. New test `TestPaycheckWizard_OpensWithEmptyForm` covers the full structural contract: visibility, employer empty, frequency default Biweekly, next-payday is FieldDate, gross amount empty and gross category seeded to Income > Salary, exactly five pre-tax labels in spec order with the first four categorized and the fifth a transfer-line (each at its expected default category index), exactly two post-tax labels with health categorized and HSA a transfer-line, primary-account select containing all four active accounts at index 0, and an empty additional-transfers list. Full suite (5234 tests) and lint stay green.

- [x] **MS-026 — Wizard menu entry**
  - RED: test `TestTransactionsMenu_NewPaycheckSchedule_Item` — the `Transactions` menu includes a `New Paycheck Schedule…` item that, when activated, opens the wizard.
  - GREEN: extend the menu definition and key handlers.
  - Confirm: tests pass.
  - Done: new `MenuActionNewPaycheckSchedule` constant added to `menubar.go` and a `{label: "New Paycheck Schedule...", action: MenuActionNewPaycheckSchedule}` row appended to the `Transactions` menu's items in `defaultMenus()` (after `Link Transfers...`). `app_menu.go`'s `handleMenuAction` gains a `MenuActionNewPaycheckSchedule` case that deactivates the menubar and dispatches `loadPaycheckWizardData()`. New async loader `App.loadPaycheckWizardData` (in `paycheck_wizard.go`) fetches active accounts via `accountSvc.List(true)` and categories via `categorySvc.List()`, runs `buildCategoryOptions` over them, and emits a new `paycheckWizardDataMsg{accounts, categoryOptions, categoryIDs}`. `App.Update` (in `app_update.go`) gains a handler that constructs the wizard via `NewPaycheckWizard(msg.categoryOptions, msg.categoryIDs, msg.accounts)` and assigns it to a new `paycheckWizard *PaycheckWizard` field on `App` (declared next to `schedPreviewDialog`). MS-026 is open-only — key routing and save logic land in MS-027/MS-028. The existing `TestMenuBar_TransactionsMenuItems` test was extended to include the new row (now expects 7 items). New test `TestTransactionsMenu_NewPaycheckSchedule_Item` covers both (a) structural — the menu's `defaultMenus()` row includes the label and action — and (b) behavioral — dispatching the action returns a `tea.Cmd` whose message is a `paycheckWizardDataMsg` carrying the seeded `Checking` account and the post-`EnsurePaycheckCategories` option list, and dispatching that message through `Update` sets `app.paycheckWizard` to a visible wizard. Full suite (5235 tests) and lint stay green.

- [ ] **MS-027 — Wizard computes remainder at save**
  - RED: test `TestPaycheckWizard_Save_ComputesRemainder` — fill out gross 5000, deductions totaling 1432.50, additional transfers 500. The "Primary deposit account" line is stored as +3067.50 on the resulting schedule.
  - GREEN: in the wizard's save handler, compute `remainder = gross − sum(deductions) − sum(additional transfers)` and write it as the primary-account line. Assemble the rest of the lines from the form.
  - Confirm: tests pass.

- [ ] **MS-028 — Wizard save creates standard multi-line schedule**
  - RED: test `TestPaycheckWizard_Save_CreatesMultiLineSchedule` — after save, the database contains a `scheduled_transactions` row with the expected scalar fields and `scheduled_split_items` children matching the wizard inputs (gross + deduction lines + transfer lines + computed remainder line).
  - GREEN: wire the wizard's save handler through the scheduled service's multi-line create path.
  - Confirm: tests pass; manual smoke: fill out the wizard, save, verify the schedule appears in the scheduled view with the right structure.

- [ ] **MS-029 — Optional "Edit as paycheck" re-open**
  - RED: test `TestScheduledDialog_EditAsPaycheck_RelaunchesWizard` — a scheduled transaction that matches the paycheck heuristic (has gross income line, ≥ 1 tax-categorized deduction line, structured similarly) shows an "Edit as paycheck →" affordance; activating it relaunches the wizard with current values pre-filled.
  - Test `TestScheduledDialog_NonPaycheckShape_HidesEditAsPaycheck` — a generic multi-line schedule that doesn't match the heuristic does not show the affordance.
  - GREEN: implement a `looksLikePaycheck(st)` heuristic and the relaunch path.
  - Confirm: tests pass.

- [ ] **MS-030 — Visual smoke check (Phase 6 milestone)**
  - Manual: invoke `Transactions → New Paycheck Schedule…`, fill out the full form per the spec example, save, verify the resulting schedule, post once via the preview dialog. Verify the paired counterparts in 401k / HSA / savings accounts. Re-open the schedule via Edit as paycheck and verify pre-fill works.

## Phase 7: Cross-Cutting Documentation

Update existing specs and the README to reflect the as-shipped behavior.

- [ ] **MS-031 — Update `specs/transactions.md`**
  - GREEN: confirm the **Split Transactions** section reflects mixed-sign semantics and the new transfer-line type. Cross-reference `specs/multiline-splits-and-paycheck.md`.
  - Document that legacy paired-single-line transfers (whole-transaction transfers) remain valid alongside new transfer-lines.

- [ ] **MS-032 — Update `specs/scheduled-transactions.md`**
  - GREEN: confirm the **Multi-Line Schedules** and **Post-Time Preview** sections describe the as-shipped behavior. Document that `amount_estimate_count` applies only to single-line schedules.

- [ ] **MS-033 — Update `specs/tui.md`**
  - GREEN: confirm the **Scheduled Preview Dialog** and **Paycheck Wizard** mockups match the implemented UI. Confirm the `Transfer →` category sentinel description in the **Category Combo Box** section. Update **Scheduled Transaction Keys** so Enter on a due item is documented as opening the preview.

- [ ] **MS-034 — Update `README.md`**
  - GREEN: add a short paragraph under the existing **Scheduled Transactions** section describing multi-line schedules, the preview dialog, and the paycheck wizard. Document the new menu entry. No new top-level section; this slots into the existing block.

- [ ] **MS-035 — Mark feature spec as implemented**
  - GREEN: add a status note at the top of `specs/multiline-splits-and-paycheck.md` indicating the v1 feature is implemented, listing any out-of-scope follow-ups.

---

## Out of Scope

The following are explicitly deferred — not in this implementation plan:

- **CLI surface for multi-line schedules and the paycheck wizard.** Multi-line creation/edit remains TUI-only. If real automation needs emerge, a spec-file approach (`tmoney scheduled paycheck create --from paycheck.toml`) is the recommended path.
- **Tax-aware reports.** Pre-tax vs post-tax distinction in reports (e.g., W-2 Box 1 reconciliation) requires a `tax_treatment` field on the category master, not on individual lines. Separate spec.
- **Migration tool to convert legacy paired-transfers into transfer-lines.** Legacy data remains valid as-is.
- **Per-line variable amounts with estimation.** Multi-line schedules use fixed template amounts; preview-time edits handle variation.
- **Bulk operations on scheduled transactions** (post-all-due, skip-all, etc.).
- **Year-end tax summary report** based on paycheck data.
- **Wizard for non-paycheck complex schedules** (e.g., mortgage payment with principal/interest/escrow). Such schedules can be created via the generic multi-line scheduled dialog.
